1. What happens when you initialise a repository? Why do you need to do it?

	.git file is generated when we initialise a repository. We need it because we need to check whether repository have change datas or not.



2. How is the staging area different from the working directory and the repository?
What value do you think it offers?

	Staging area is basically a loading dock where you get to determine what changes get shipped away. The working directory is simply a folder on the computer. The value the staging area offers us that it allows the user to group multiple files together for one commit.



3. How can you use the staging area to make sure you have one commit per logical
change?




What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

How do the diagrams help you visualize the branch structure?

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?
